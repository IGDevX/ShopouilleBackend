apiVersion: v2
name: monitoring
description: Umbrella chart for Prometheus, Grafana, Alertmanager, dashboards & alerts
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: kube-prometheus-stack
    version: 65.0.0            # pin a recent stable (adjust as needed)
    repository: https://prometheus-community.github.io/helm-charts

