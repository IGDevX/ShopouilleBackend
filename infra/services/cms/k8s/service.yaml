iapiVersion: v1
kind: Service
metadata:
  name: cms
  namespace: default
  labels:
    app: cms
spec:
  selector:
    app: cms
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 9090

